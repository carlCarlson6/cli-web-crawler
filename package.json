{
  "name": "cli-monorepo",
  "private": true,
  "workspaces": [
    "cli-be",
    "cli-web",
    "cli-contracts"
  ],
    "scripts": {
    "build": "npm run build -ws",
    "build:be": "npm run build -w cli-contracts && npm run build -w cli-be",
    "dev": "npm run dev -ws",
    "lint": "npm run lint -ws"
  }
}